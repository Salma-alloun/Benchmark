# Benchmark des structures de données

Ce projet contient le code pour comparer les structures de données suivantes en C++ :
- unordered_set
- set
- map
- BST simple
- Red-Black Tree

Pour compiler :
g++ main.cpp -o benchmark
./benchmark
